{
  "name": "my-blog22",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:backend": "yarn --cwd backend develop",
    "dev:frontend": "wait-on http://localhost:1337/admin && yarn --cwd frontend dev",
    "dev": "cross-env FORCE_COLOR=1 npm-run-all -l -p develop:*"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "wait-on": "5.2.1",
    "cross-env": "7.0.3"
  }
}
